---
import HyperobjectsContent from '../../../components/concepts/hyperobjects.astro';
import { getTranslations } from '../../../lib/i18n';

// The three "Choose Your Path" cards are the homepage's primary CTAs. Without
// these routes an English visitor clicking them landed on the Spanish page.
const lang = 'en';
const t = getTranslations(lang);
---

<HyperobjectsContent lang={lang} t={t} />
