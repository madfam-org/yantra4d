---
import CommonsContent from '../../components/concepts/commons.astro';
import { getTranslations } from '../../lib/i18n';

const lang = Astro.currentLocale || 'es';
const t = getTranslations(lang);
---

<CommonsContent lang={lang} t={t} />
