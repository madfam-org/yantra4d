---
import CommonDenominatorContent from '../../components/concepts/common-denominator.astro';
import { getTranslations } from '../../lib/i18n';

const lang = Astro.currentLocale || 'es';
const t = getTranslations(lang);
---

<CommonDenominatorContent lang={lang} t={t} />
