apiVersion: v1
kind: Service
metadata:
  name: yantra4d-landing
  labels:
    app.kubernetes.io/name: yantra4d-landing
    app.kubernetes.io/part-of: yantra4d
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: yantra4d-landing
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
